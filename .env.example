DATABASE_URL="postgresql://postgres:postgres@localhost:5432/ai_prompt_library"

# GitHub OAuth app (create at https://github.com/settings/developers).
# Callback URL: http://localhost:3000/api/auth/callback/github
AUTH_GITHUB_ID=""
AUTH_GITHUB_SECRET=""

# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
AUTH_SECRET=""

# Optional: email of the account that should be auto-promoted to ADMIN on first sign-in.
ADMIN_EMAIL=""
